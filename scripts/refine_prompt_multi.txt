# scripts/refine_prompt_multi.txt

#lang=ja
ja:
#-------------------【必読：システム命令】-------------------
# ・この出力は必ず**日本語**で行ってください。  
# ・以下の「指示」部分のみを参照し、「例示」部分は絶対に出力しないでください。
# ・勝手な補完・想像・推測は禁止です。  
# ・OCR抽出とPDF抽出の両方から読み取れたテキストを忠実に反映します。  
# ・読めない部分は「/*…*/」で囲み、必ず「不明瞭箇所一覧」に箇条書きで挙げてください。

#-------------------【指示セクション】-------------------

あなたはプロの技術ライターです。以下の手順で、このページの内容を再構成してください。

1. ページ識別  
   - ページ内に「文書番号」があれば、`### 文書番号` セクションとして出力し、実際の番号を記載。  

2. 図表／写真観察  
   - ページ内に「図N」「Fig. N」などのキャプションがあれば、`### 図表観察` セクションを作成し、  
     - `図N キャプション本文` の形式で **原文通り** にリスト化。  

3. 本文（段落）  
   - OCRで**確実に**読み取れた行をすべて列挙し、  
   - 最後にそれらを一つの**日本語段落**にまとめる。  
   - **不要な改行／空白は削除**し、自然な文章に整形。  
   - **文字化け・不明瞭**と判断した文字列は `/*…*/` で囲んだまま残す。  

4. 不明瞭箇所一覧  
   - 本文中に出現した `/*…*/` をすべて抽出し、`### 不明瞭箇所一覧` セクションで箇条書き。  
   - もし存在しなければ「なし」と記載。  

5. 出力順序  
   必ず以下の順番で出力してください。該当しないセクションは完全に省略します。

