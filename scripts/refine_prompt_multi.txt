#lang=ja
以下は、元文章がPDFの場合、同一の文書についてPDF文書からの文字情報と、OCRによって画像から抽出したテキスト情報を併せた原文です。

文書の意味を正確に保ったまま、整った日本語として再構成してください。  
抽出結果の区分（例：【PDF抽出】や【OCR抽出】など）はすべて削除してください。  
重複する内容や文書構造の重なりがある場合は、自然に統合してください。

以下の制約に従ってください：
- 原文中のラベルや区切り記号（例：【OCR抽出】など）は出力に含めないこと
- 表現を整える際に、曖昧な言い回しや注釈（例：「誤認の可能性があります」など）は使わないこと
- 可能な限り自然な段落構成で、情報を網羅的に再構成すること
- 数値、記号、固有名詞などは、原文通り正確に保つこと
- 明らかに誤っている語句、読み取り困難な語、不自然な語はすべて前後を /* */ で囲んでください。これらの印は後の処理で重要な役割を果たします。

---

# 原文：
{TEXT}

# 出力（整形済み日本語）：

---

#lang=en
The following text combines two sources for the same document:  
- Characters extracted directly from the PDF layer, and  
- Text extracted from page images via OCR.  

Re-write the content in natural, well-structured English while preserving the original meaning.  
Remove all source labels such as 【PDF Extract】 or 【OCR Extract】, and seamlessly integrate any duplicated or overlapping sections into a single coherent passage.

Follow these constraints:  
- Do **not** include any labels or delimiters (e.g., 【OCR Extract】) in the output  
- Avoid vague wording or disclaimers (e.g., “there may be recognition errors”)  
- Re-organise the information into intuitive paragraphs that cover the full content  
- Keep numbers, symbols, and proper nouns exactly as they appear in the original text  
- If a word or phrase seems inaccurate or unreadable, enclose it with `/* */` to mark the uncertainty.  Enclose all clearly incorrect words, unreadable phrases, or unnatural expressions with `/* */`. These markers play an important role in subsequent processing.


---

# Original Text:
{TEXT}

# Output (Refined English):
